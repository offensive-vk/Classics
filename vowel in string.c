#include<stdio.h>
#include<string.h>

// wap is total number of vowel present in the given string.
int main(){
	int count=0, i,j, length;
	char str[100];
	printf("Enter String : ");
	gets(str);
	length = strlen(str);
	for(i=0; i<length; i++)
	{
		printf("%s", i);
	}
    
}
